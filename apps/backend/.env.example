# Server
PORT=3000
NODE_ENV=development

# Database
MONGODB_URI=mongodb://localhost:27017/thewritingapp

# Auth — MUST be set, no fallback
JWT_SECRET=replace_with_a_strong_random_secret

# Frontend origin for CORS
FRONTEND_URL=http://localhost:5173
